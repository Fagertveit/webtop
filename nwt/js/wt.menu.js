/* * * * * * * *
 * Core - Menu *
 * * * * * * * *
 * This class takes care about portal menu bars and all it's sub elements.
 */
 
WT.menu = {

	MenuBar : function(params) {
		var menubar = {
			constructor : function(params) {

			},

			addEventListeners : function() {

			}
		};
		menubar.constructor(params);

		return menubar;
	},

	Menu : function(params) {
		var menu = {
			constructor : function(params) {

			}
		};
		menu.constructor(params);

		return menu;
	},

	MenuItem : function(params) {
		var menuitem = {
			constructor : function(params) {

			}
		};
		menuitem.constructor(params);

		return menuitem;
	},

	SubMenu : function(params) {
		var submenu = {
			constructor : function(params) {

			}
		};
		submenu.constructor(params);

		return submenu;
	}
};